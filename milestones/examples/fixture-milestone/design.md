# Fixture milestone design

This harmless fixture proves deterministic milestone locking, dependency ordering, persistence, retry handling, and prompt rendering. It does not authorize source execution.

The intended sequence is one marker contract followed by a dependent second marker contract and a final validation contract.
